# 2026-07-22 工作日志

- 根据 `招标信息汇总.md` 生成可视化 HTML 文件 `招标信息可视化.html`，按优先级（高→中→低）排序，包含统计卡片、优先级分布图、产品类别 TOP5 图表、搜索筛选与导出功能。
- 在 `招标信息可视化.html` 中新增「一手平台」列，同步数据源中的平台信息，并支持搜索与 CSV 导出。
- 为「一手平台」添加官方平台超链接（中车购2.0平台､国铁采购平台､北京市公共资源交易中心､上海地铁采购平台､深圳地铁智能招采管理平台），点击可跳转。
- 自动化同步：检测到 `招标信息汇总.md` 更新，同步刷新 `招标信息可视化.html` 数据，剔除截止日期已过去 3 天的 2 条记录，保留 41 条有效招标信息。
- 再次同步：新增 1 条招标信息（四方所-复兴号智能动车组充电机智能测试试验台），屏蔽 2 条已过期记录，最终保留 42 条。
- 恢复 HTML：检测到 `招标信息可视化.html` 与 `招标信息汇总.md` 不一致，已按源文件重新生成，恢复为 44 条招标信息，并保留一手平台超链接等现有功能。
- 调整表格样式：为各列设置固定宽度（项目名称/招标单位加宽，产品类别收窄），并将表格正文字号从 text-sm 放大至 0.9375rem，提升可读性。
- 自动化同步（2026-07-22 10:49）：解析 `招标信息汇总.md` 中 44 条记录，按规则过滤过期记录（截止时间超过今日 3 天），丢弃 2 条已过期，保留 42 条；按优先级→发布时间排序后替换 HTML 中 `const tenders` 数组，保留其他所有代码不变。
