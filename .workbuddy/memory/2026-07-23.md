# 2026-07-23 工作日志

- 备份 `招标信息可视化.html` 为 `招标信息可视化_backup_20260723.html`。
- 新增“人工批阅”功能：
  - 每条招标信息支持三种状态：待查阅（默认）､已查阅､待跟进，使用 localStorage 以公告链接为键持久化存储。
  - 表格每行最右侧新增“状态操作”列，点击按钮循环切换状态，按钮颜色和图标随状态变化。
  - “待查阅”行使用浅红色背景高亮。
  - 新增三个审阅状态统计卡片（待查阅､已查阅､待跟进），实时更新。
  - 筛选栏新增状态筛选按钮（全部状态､待查阅､已查阅､待跟进）。
  - 增加“全部标记已阅”按钮，带确认弹窗，仅将当前待查阅项设为已查阅。
  - CSV 导出增加“审阅状态”列。
- 已通过 Node.js 脚本对 HTML 中的 JavaScript 进行语法校验，无报错。
- 修复“人工批阅”状态同步问题：
  - 为 `tenders` 数组中 42 条记录添加唯一 `id` 字段（1–42）。
  - 将审阅状态的存储/读取/切换键从 `link` 改为 `id`，避免同 link 的多条记录状态联动。
  - 表格状态按钮使用 `data-id`，事件监听传入 `parseInt(btn.dataset.id, 10)`。
  - 统计卡片、筛选、导出、批量标记已阅均同步改为基于 `id`。
  - 备份当前文件为 `招标信息可视化_backup_20260723_2.html`。
  - 已通过 Node.js 模拟测试验证：同 link 的不同记录可独立变更状态，markAllAsRead 不影响非待查阅项。
- 接入后端 API（工作文件已重命名为 `index.html`）：
  - 删除 localStorage 相关代码，改用内存变量 `statuses` 和 `keywords`。
  - 新增 `API_BASE` 与 4 个 API 函数：`loadStatuses`、`saveStatuses`、`loadKeywords`、`saveKeywords`。
  - 页面初始化改为 `DOMContentLoaded` 异步加载：先 `loadStatuses()` 和 `loadKeywords()`，再渲染表格与图表。
  - 新增“关键词管理”面板：展示关键词标签、添加/删除、实时同步后端。
  - 备份当前文件为 `index_backup_20260723_api.html`。
  - 已通过 Node.js 模拟后端测试：加载/保存状态、加载/保存关键词、markAllAsRead 逻辑均正常。
- 优化关键词管理面板为折叠/展开样式：
  - 默认收起，标题栏显示“📋 关键词管理（共 N 个）”。
  - 点击标题栏切换展开/收起，箭头旋转（▶/▼），内容区使用 max-height + opacity 平滑过渡。
  - 展开后以 Tag 形式横向排列关键词，每个标签带“✕”删除按钮。
  - 底部保留输入框、添加按钮，增删后调用 `saveKeywords()` 同步后端。
  - 备份当前文件为 `index_backup_20260723_collapse.html`。
  - 已通过 JavaScript 语法校验和 API 集成测试。
- 调整关键词管理保存逻辑为批量确认：
  - 移除 `addKeyword()` 和 `removeKeyword()` 中的实时 `saveKeywords()` 调用。
  - 面板底部增加绿色“确认修改”按钮，点击后才统一调用 `saveKeywords(keywords)`。
  - 同步成功后显示“✅ 关键词已同步”提示，2 秒后自动消失。
  - 说明文字改为“用于下次获取招标信息的关键词”。
  - 备份当前文件为 `index_backup_20260723_confirm.html`。
  - 已通过 JavaScript 语法校验和 API 集成测试。
